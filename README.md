HTTP-based integration layer for the TauP Toolkit
-------------------------------------------------

For now, the TauP-x.x.x.jar file should be added to lib/ manually (treated as an unmanaged dependency).
